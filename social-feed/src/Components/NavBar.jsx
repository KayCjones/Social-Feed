import React from 'react';

const NavBar = (props) => {
    return (  
        <header>
            <div>
                "Social Feed"
            </div>
        </header>
    );
}
 
export default NavBar;